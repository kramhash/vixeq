## API Report File for "@vixeq/core"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

// @public (undocumented)
export const addTimelineEvent: (project: TimelineProject, event: Partial<TimelineEvent>, eventValidator?: TimelineEventValidator) => TimelineProject;

// @public (undocumented)
export const addTimelineTrack: (project: TimelineProject, track: Partial<TimelineTrack>) => TimelineProject;

// @public
export const addTrack: (project: SequenceProject, name?: string) => SequenceProject;

// @public (undocumented)
export const arrangementDurationBeats: (arrangement: ArrangementProject) => number;

// @public
export class ArrangementEngine {
    constructor(arrangement: ArrangementProject, options?: ArrangementEngineOptions);
    // (undocumented)
    dispose(): void;
    // (undocumented)
    getArrangement(): ArrangementProject;
    // (undocumented)
    getCurrentSection(): ArrangementSection | null;
    // (undocumented)
    getPlaybackState(): PlaybackState;
    // (undocumented)
    getPosition(): ChannelPosition;
    // (undocumented)
    on<TEventName extends ArrangementEventName>(eventName: TEventName, handler: ArrangementEventHandler<TEventName>): Unsubscribe;
    // (undocumented)
    pause(): Promise<void>;
    // (undocumented)
    play(): Promise<void>;
    // (undocumented)
    sampleChannels(easing?: EasingFunction): Record<string, number>;
    // (undocumented)
    sampleChannelsAt(timeMs: number, easing?: EasingFunction): Record<string, number>;
    // (undocumented)
    seekBeat(beat: number): Promise<void>;
    // (undocumented)
    seekPositionMs(positionMs: number): Promise<void>;
    setArrangement(arrangement: ArrangementProject): void;
    // (undocumented)
    setLoop(loop: boolean): void;
    // (undocumented)
    stop(): Promise<void>;
}

// @public (undocumented)
export type ArrangementEngineOptions = {
    transport?: PlaybackTransport;
    lookaheadMs?: number;
    loop?: boolean;
    missedStepPolicy?: MissedStepPolicy;
    onStep?: ArrangementEventHandler<"step">;
    onSection?: ArrangementEventHandler<"section">;
    onListenerError?: (error: unknown, context: ListenerErrorContext) => void;
};

// @public (undocumented)
export type ArrangementEventHandler<TEventName extends ArrangementEventName> = (event: ArrangementEventMap[TEventName]) => void;

// @public (undocumented)
export type ArrangementEventMap = {
    step: StepEvent;
    playback: ArrangementPlaybackEvent;
    project: ArrangementProjectEvent;
    section: ArrangementSectionEvent;
};

// @public (undocumented)
export type ArrangementEventName = keyof ArrangementEventMap;

// @public (undocumented)
export type ArrangementMigrationOptions = {
    durationBeats?: number;
};

// @public (undocumented)
export type ArrangementMigrationResult = MigrationResult<ArrangementProject>;

// @public (undocumented)
export type ArrangementPlaybackEvent = Omit<EnginePlaybackEvent, "snapshot"> & {
    snapshot: ArrangementPlaybackSnapshot;
};

// @public (undocumented)
export type ArrangementPlaybackSnapshot = EnginePlaybackSnapshot & {
    section: ArrangementSection | null;
};

// @public
export type ArrangementProject = {
    version: 2;
    timing: TimingMap;
    durationBeats: number;
    patterns: Record<string, SequenceProject>;
    sections: ArrangementSection[];
};

// @public (undocumented)
export type ArrangementProjectEvent = {
    arrangement: ArrangementProject;
    previousArrangement: ArrangementProject;
    changedChannelIds: string[];
    previousChannels: Record<string, number>;
    channels: Record<string, number>;
    positionMs: number;
    beat: number;
};

// @public (undocumented)
export type ArrangementProjectV1 = {
    version: 1;
    bpm: number;
    patterns: Record<string, SequenceProject>;
    sections: ArrangementSection[];
};

// @public
export type ArrangementSection = {
    id: string;
    patternId: string;
    startBeat: number;
    endBeat: number;
};

// @public
export type ArrangementSectionEvent = {
    section: ArrangementSection | null;
    beat: number;
    positionMs: number;
    transportPositionMs: number;
    lateByMs: number;
    cause: StepEventCause;
};

// @public (undocumented)
export type AudioBufferTransportOptions = PlaybackTransportBaseOptions & {
    destination?: AudioNode;
    loop?: boolean;
};

// @public
export const beatToMs: (timing: TimingMap, beat: number) => number;

// @public (undocumented)
export const browserClock: PlaybackClock;

// @public (undocumented)
export type ChannelPosition = {
    positionMs: number;
    beat: number;
};

// @public (undocumented)
export type ChannelProjectEvent = {
    changedChannelIds: string[];
    previousChannels: Record<string, number>;
    channels: Record<string, number>;
    positionMs: number;
    beat: number;
};

// @public
export type ChannelSource = {
    on(eventName: "step", handler: SequencerEventHandler<"step">): Unsubscribe;
    sampleChannels(easing?: EasingFunction): Record<string, number>;
    sampleChannelsAt(timeMs: number, easing?: EasingFunction): Record<string, number>;
    getPosition(): ChannelPosition;
    getPlaybackState(): PlaybackState;
    on(eventName: "playback", handler: (event: EnginePlaybackEvent) => void): Unsubscribe;
    on(eventName: "project", handler: (event: ChannelProjectEvent) => void): Unsubscribe;
};

// @public (undocumented)
export const clamp: (value: number, min: number, max: number) => number;

// @public (undocumented)
export const clamp01: (value: number) => number;

// @public (undocumented)
export const clampStepValue: (value: number) => StepValue;

// @public (undocumented)
export const clearTrack: (project: SequenceProject, trackId: string) => SequenceProject;

// @public (undocumented)
export type ClockTransportOptions = PlaybackTransportBaseOptions & {
    durationMs?: number;
    loop?: boolean;
};

// @public (undocumented)
export const createArrangement: (options?: CreateArrangementOptions) => ArrangementProject;

// @public (undocumented)
export type CreateArrangementOptions = {
    timing?: CreateTimingMapOptions | TimingMap;
    durationBeats?: number;
    patterns?: Record<string, SequenceProject>;
    sections?: ArrangementSection[];
};

// @public
export function createAudioBufferTransport(audioContext: AudioContext, buffer: AudioBuffer, options?: AudioBufferTransportOptions): PlaybackTransport;

// @public (undocumented)
export function createClockTransport(clock: PlaybackClock, options?: ClockTransportOptions): PlaybackTransport;

// @public
export const createDecayEnvelope: (config: SmoothingConfig) => Envelope;

// @public
export const createEnvelope: (options?: CreateEnvelopeOptions) => Envelope;

// @public (undocumented)
export type CreateEnvelopeOptions = {
    attack?: number;
    decay?: number;
    curve?: EasingFunction;
    peak?: number;
};

// @public
export function createMediaElementTransport(media: HTMLMediaElement, options?: MediaElementTransportOptions): PlaybackTransport;

// @public
export const createProject: (options?: CreateProjectOptions) => SequenceProject;

// @public (undocumented)
export type CreateProjectOptions = {
    bpm?: number;
    stepCount?: number;
    stepsPerBeat?: number;
    trackCount?: number;
    trackNames?: string[];
};

// @public (undocumented)
export const createTimelineProject: (options?: CreateTimelineProjectOptions) => TimelineProject;

// @public (undocumented)
export type CreateTimelineProjectOptions = {
    timing?: CreateTimingMapOptions | TimingMap;
    durationBeats?: number;
    tracks?: Partial<TimelineTrack>[];
    events?: Partial<TimelineEvent>[];
};

// @public (undocumented)
export const createTimingMap: (options?: CreateTimingMapOptions) => TimingMap;

// @public (undocumented)
export type CreateTimingMapOptions = {
    bpm: number;
    startPositionMs?: number;
} | {
    tempos: TempoEvent[];
    startPositionMs?: number;
};

// @public
export const createTrack: (options?: {
    name?: string;
    stepCount?: number;
    steps?: number[];
    enabled?: boolean;
}) => Track;

// @public (undocumented)
export const decaySmoothedValue: (current: number, deltaSeconds: number, config: SmoothingConfig) => number;

// @public
export const easeInCubic: EasingFunction;

// @public
export const easeInOutCubic: EasingFunction;

// @public
export const easeInOutQuad: EasingFunction;

// @public
export const easeInQuad: EasingFunction;

// @public
export const easeOutCubic: EasingFunction;

// @public
export const easeOutQuad: EasingFunction;

// @public (undocumented)
export type EasingFunction = (t: number) => number;

// @public (undocumented)
export type EnginePlaybackCause = "command" | "transport" | "local-end";

// @public (undocumented)
export type EnginePlaybackEvent = {
    type: "play" | "pause" | "stop" | "seek" | "ratechange" | "loopchange" | "loop" | "durationchange" | "bufferingchange" | "ended" | "error";
    cause: EnginePlaybackCause;
    previousState: PlaybackState;
    snapshot: EnginePlaybackSnapshot;
    error?: unknown;
};

// @public (undocumented)
export type EnginePlaybackSnapshot = {
    state: PlaybackState;
    positionMs: number;
    beat: number;
    playbackRate: number;
    projectLoop: boolean;
    transportLoop: boolean;
    buffering: boolean;
};

// @public
export type Envelope = {
    trigger(timeMs: number, value?: number): void;
    sample(timeMs: number): number;
    reset(): void;
};

// @public (undocumented)
export const exciteSmoothedValue: (current: number, input: number, config: SmoothingConfig) => number;

// @public (undocumented)
export const getEventsAtBeat: (project: TimelineProject, beat: number, toleranceBeats?: number, options?: TimelineQueryOptions) => TimelineEvent[];

// @public
export const getEventsInBeatRange: (project: TimelineProject, fromBeat: number, toBeat: number, options?: TimelineQueryOptions) => TimelineEvent[];

// @public (undocumented)
export const getNextEvents: (project: TimelineProject, beat: number, count: number, options?: TimelineQueryOptions) => TimelineEvent[];

// @public (undocumented)
export type JsonObject = {
    [key: string]: JsonValue;
};

// @public (undocumented)
export type JsonPrimitive = string | number | boolean | null;

// @public (undocumented)
export type JsonValue = JsonPrimitive | JsonValue[] | JsonObject;

// @public
export const lerp: (from: number, to: number, t: number) => number;

// @public
export const linear: EasingFunction;

// @public (undocumented)
export type ListenerErrorContext = {
    source: "transport" | "engine";
    eventName: string;
};

// @public (undocumented)
export type MediaElementTransportOptions = PlaybackTransportBaseOptions & {
    audioContext?: AudioContext;
};

// @public (undocumented)
export const migrateArrangementProject: (input: unknown, options?: ArrangementMigrationOptions) => ArrangementMigrationResult;

// @public
export const migrateTimelineProject: (project: TimelineProjectV1, options: TimelineMigrationOptions) => MigrationResult<TimelineProject>;

// @public (undocumented)
export type MigrationResult<T> = {
    ok: true;
    project: T;
    warnings: MigrationIssue[];
} | {
    ok: false;
    errors: MigrationIssue[];
};

// @public (undocumented)
export type MissedStepPolicy = "emit" | "skip";

// @public
export const msToBeat: (timing: TimingMap, ms: number) => number;

// @public
export const normalizeArrangement: (input: unknown) => ArrangementProject;

// @public (undocumented)
export const normalizeProject: (input: unknown) => SequenceProject;

// @public (undocumented)
export const normalizeTempoEvent: (tempo: Partial<TempoEvent>, fallbackBeat?: number) => TempoEvent;

// @public (undocumented)
export const normalizeTempos: (tempos: Partial<TempoEvent>[] | undefined) => TempoEvent[];

// @public
export const normalizeTimelineEvent: (event?: Partial<TimelineEvent>, index?: number) => TimelineEvent;

// @public (undocumented)
export const normalizeTimelineProject: (input: unknown) => TimelineProject;

// @public
export const normalizeTimelineTrack: (track?: Partial<TimelineTrack>, index?: number) => TimelineTrack;

// @public (undocumented)
export const normalizeTimingMap: (input: Partial<TimingMap> | CreateTimingMapOptions | undefined) => TimingMap;

// @public (undocumented)
export type PlaybackClock = {
    now(): number;
    setTimer(callback: () => void, delayMs: number): unknown;
    clearTimer(timerId: unknown): void;
};

// @public (undocumented)
export class PlaybackError extends Error {
    constructor(code: PlaybackErrorCode, message?: string);
    // (undocumented)
    readonly code: PlaybackErrorCode;
}

// @public (undocumented)
export type PlaybackErrorCode = "TRANSPORT_DISPOSED" | "DURATION_UNAVAILABLE";

// @public (undocumented)
export type PlaybackOperation = "play" | "pause" | "stop" | "seek" | "ratechange" | "transport-loop";

// @public (undocumented)
export type PlaybackSnapshot = {
    state: PlaybackState;
    positionMs: number;
    durationMs: number | null;
    playbackRate: number;
    loop: boolean;
    buffering: boolean;
};

// @public (undocumented)
export type PlaybackState = "stopped" | "playing" | "paused" | "ended";

// @public (undocumented)
export type PlaybackTransport = {
    getSnapshot(): PlaybackSnapshot;
    getPlaybackState(): PlaybackState;
    getPositionMs(): number;
    getDurationMs(): number | null;
    getPlaybackRate(): number;
    getLoop(): boolean;
    play(): Promise<void>;
    pause(): Promise<void>;
    stop(): Promise<void>;
    seekMs(positionMs: number): Promise<void>;
    setPlaybackRate(rate: number): Promise<void>;
    setLoop(loop: boolean): Promise<void>;
    subscribe(listener: (event: PlaybackTransportEvent) => void): () => void;
    dispose(): void;
};

// @public (undocumented)
export type PlaybackTransportBaseOptions = {
    onListenerError?: (error: unknown, context: ListenerErrorContext) => void;
};

// @public (undocumented)
export type PlaybackTransportEvent = {
    type: "play";
    snapshot: PlaybackSnapshot;
} | {
    type: "pause";
    snapshot: PlaybackSnapshot;
} | {
    type: "stop";
    snapshot: PlaybackSnapshot;
} | {
    type: "seek";
    previousPositionMs: number;
    snapshot: PlaybackSnapshot;
} | {
    type: "ratechange";
    previousPlaybackRate: number;
    snapshot: PlaybackSnapshot;
} | {
    type: "loopchange";
    previousLoop: boolean;
    snapshot: PlaybackSnapshot;
} | {
    type: "loop";
    iteration: number;
    snapshot: PlaybackSnapshot;
} | {
    type: "durationchange";
    previousDurationMs: number | null;
    snapshot: PlaybackSnapshot;
} | {
    type: "bufferingchange";
    previousBuffering: boolean;
    snapshot: PlaybackSnapshot;
} | {
    type: "ended";
    snapshot: PlaybackSnapshot;
} | {
    type: "error";
    error: unknown;
    snapshot: PlaybackSnapshot;
} | {
    type: "dispose";
    snapshot: PlaybackSnapshot;
};

// @public
export const presets: Record<string, SequenceProject>;

// @public (undocumented)
export type ProjectEvent = {
    project: SequenceProject;
    previousProject: SequenceProject;
    stepIndex: number;
    changedChannelIds: string[];
    previousChannels: Record<string, number>;
    channels: Record<string, number>;
    positionMs: number;
    beat: number;
    timestamp?: number;
};

// @public
export const randomizeTrack: (project: SequenceProject, trackId: string, options?: RandomizeTrackOptions) => SequenceProject;

// @public (undocumented)
export type RandomizeTrackOptions = {
    probability?: number;
    min?: number;
    max?: number;
    random?: () => number;
};

// @public (undocumented)
export const removeTimelineEvent: (project: TimelineProject, eventId: string) => TimelineProject;

// @public (undocumented)
export const removeTimelineTrack: (project: TimelineProject, trackId: string) => TimelineProject;

// @public (undocumented)
export const removeTrack: (project: SequenceProject, trackId: string) => SequenceProject;

// @public (undocumented)
export const renameTrack: (project: SequenceProject, trackId: string, name: string) => SequenceProject;

// @public
export const resolveArrangementStep: (arrangement: ArrangementProject, beat: number) => ResolvedArrangementStep | null;

// @public (undocumented)
export type ResolvedArrangementStep = {
    section: ArrangementSection;
    stepIndex: number;
    nextStepIndex: number;
    phase: number;
};

// @public
export const rotateTrackSteps: (project: SequenceProject, trackId: string, offset: number) => SequenceProject;

// @public
export const sampleArrangement: (arrangement: ArrangementProject, beat: number, easing?: EasingFunction, trackIds?: string[]) => Record<string, number>;

// @public
export const sectionAtBeat: (arrangement: ArrangementProject, beat: number) => SectionLookup | null;

// @public (undocumented)
export type SectionLookup = {
    section: ArrangementSection;
    localBeat: number;
};

// @public (undocumented)
export type SequenceProject = {
    version: 1;
    bpm: number;
    stepCount: number;
    stepsPerBeat: number;
    tracks: Track[];
};

// @public (undocumented)
export const SEQUENCER_LIMITS: {
    readonly minBpm: 20;
    readonly maxBpm: 300;
    readonly minTracks: 1;
    readonly maxTracks: 16;
    readonly minStepCount: 1;
    readonly maxStepCount: 128;
    readonly minStepsPerBeat: 1;
    readonly maxStepsPerBeat: 32;
    readonly defaultBpm: 120;
    readonly defaultStepCount: 16;
    readonly defaultTrackCount: 4;
    readonly defaultStepsPerBeat: 4;
};

// @public
export class SequencerEngine {
    constructor(project: SequenceProject, options?: SequencerEngineOptions);
    dispose(): void;
    // (undocumented)
    getCurrentStepIndex(): number;
    // (undocumented)
    getPlaybackState(): PlaybackState;
    // (undocumented)
    getPosition(): ChannelPosition;
    // (undocumented)
    getProject(): SequenceProject;
    // (undocumented)
    on<TEventName extends SequencerEventName>(eventName: TEventName, handler: SequencerEventHandler<TEventName>): Unsubscribe;
    pause(): Promise<void>;
    play(): Promise<void>;
    sampleChannels(easing?: EasingFunction): Record<string, number>;
    sampleChannelsAt(timeMs: number, easing?: EasingFunction): Record<string, number>;
    seekPositionMs(positionMs: number): Promise<void>;
    seekStep(stepIndex: number): Promise<void>;
    setProject(project: SequenceProject): void;
    stop(): Promise<void>;
}

// @public (undocumented)
export type SequencerEngineOptions = {
    transport?: PlaybackTransport;
    lookaheadMs?: number;
    missedStepPolicy?: MissedStepPolicy;
    onStep?: SequencerEventHandler<"step">;
    onListenerError?: (error: unknown, context: ListenerErrorContext) => void;
};

// @public (undocumented)
export type SequencerEventHandler<TEventName extends SequencerEventName> = (event: SequencerEventMap[TEventName]) => void;

// @public (undocumented)
export type SequencerEventMap = {
    step: StepEvent;
    playback: SequencerPlaybackEvent;
    project: ProjectEvent;
};

// @public (undocumented)
export type SequencerEventName = keyof SequencerEventMap;

// @public (undocumented)
export type SequencerPlaybackEvent = Omit<EnginePlaybackEvent, "snapshot"> & {
    snapshot: SequencerPlaybackSnapshot;
};

// @public (undocumented)
export type SequencerPlaybackSnapshot = EnginePlaybackSnapshot & {
    stepIndex: number;
};

// @public (undocumented)
export type SequencerTransport = {
    clock: PlaybackClock;
    play(): void | Promise<void>;
    stop(): void | Promise<void>;
    pause?(): void | Promise<void>;
    seek?(timeMs: number): void | Promise<void>;
    dispose?(): void;
};

// @public (undocumented)
export const setProjectBpm: (project: SequenceProject, bpm: number) => SequenceProject;

// @public
export const setStepValue: (project: SequenceProject, trackId: string, stepIndex: number, value: number) => SequenceProject;

// @public (undocumented)
export const setTimelineTrackEnabled: (project: TimelineProject, trackId: string, enabled: boolean) => TimelineProject;

// @public (undocumented)
export const setTrackEnabled: (project: SequenceProject, trackId: string, enabled: boolean) => SequenceProject;

// @public (undocumented)
export type SmoothingConfig = {
    decayRate: number;
    impact: number;
    lift: number;
    rest?: number;
    holdWhenInputZero?: boolean;
};

// @public (undocumented)
export const sortTimelineEvents: (events: TimelineEvent[]) => TimelineEvent[];

// @public (undocumented)
export type StepEvent = {
    stepIndex: number;
    bpm: number;
    scheduledPositionMs: number;
    transportPositionMs: number;
    lateByMs: number;
    durationMs: number;
    cause: StepEventCause;
    tracks: StepEventTrack[];
    timestamp?: number;
};

// @public
export type StepEventCause = "play" | "tick" | "seek" | "project-change" | "loop";

// @public (undocumented)
export type StepEventTrack = {
    id: string;
    name: string;
    enabled: boolean;
    value: StepValue;
    nextValue: StepValue;
};

// @public (undocumented)
export type StepValue = number;

// @public (undocumented)
export type TempoEvent = {
    beat: number;
    bpm: number;
};

// @public (undocumented)
export type TempoEventV1 = {
    beat: number;
    bpm: number;
};

// @public (undocumented)
export type TimelineCueEvent<TEvent extends TimelineEvent = TimelineEvent> = {
    event: TEvent;
    iteration: number;
    scheduledPositionMs: number;
    transportPositionMs: number;
    lateByMs: number;
};

// @public
export class TimelineEngine<TEvent extends TimelineEvent = TimelineEvent> {
    constructor(project: TimelineProject<TEvent>, options?: TimelineEngineOptions<TEvent>);
    // (undocumented)
    dispose(): void;
    // (undocumented)
    getPlaybackState(): PlaybackState;
    // (undocumented)
    getPosition(): {
        positionMs: number;
        beat: number;
    };
    // (undocumented)
    getProject(): TimelineProject<TEvent>;
    // (undocumented)
    on<TEventName extends TimelineEventName>(eventName: TEventName, handler: TimelineEventHandler<TEventName, TEvent>): Unsubscribe;
    // (undocumented)
    pause(): Promise<void>;
    // (undocumented)
    play(): Promise<void>;
    // (undocumented)
    seekBeat(beat: number): Promise<void>;
    // (undocumented)
    seekPositionMs(positionMs: number): Promise<void>;
    // (undocumented)
    setLoop(loop: boolean): void;
    // (undocumented)
    setProject(project: TimelineProject<TEvent>): void;
    // (undocumented)
    stop(): Promise<void>;
}

// @public (undocumented)
export type TimelineEngineOptions<TEvent extends TimelineEvent = TimelineEvent> = {
    transport?: PlaybackTransport;
    lookaheadMs?: number;
    loop?: boolean;
    missedCuePolicy?: MissedStepPolicy;
    eventValidator?: TimelineEventValidator<TEvent>;
    onCue?: TimelineEventHandler<"cue", TEvent>;
    onListenerError?: (error: unknown, context: ListenerErrorContext) => void;
};

// @public (undocumented)
export type TimelineEvent<TType extends string = string, TData extends JsonObject = JsonObject> = {
    id: string;
    trackId: string | null;
    beat: number;
    type: TType;
    data?: TData;
};

// @public (undocumented)
export type TimelineEventHandler<TEventName extends TimelineEventName, TEvent extends TimelineEvent = TimelineEvent> = (event: TimelineEventMap<TEvent>[TEventName]) => void;

// @public (undocumented)
export type TimelineEventMap<TEvent extends TimelineEvent = TimelineEvent> = {
    cue: TimelineCueEvent<TEvent>;
    playback: TimelinePlaybackEvent;
    project: TimelineProjectEvent<TEvent>;
};

// @public (undocumented)
export type TimelineEventName = keyof TimelineEventMap;

// @public (undocumented)
export type TimelineEventV1 = {
    id: string;
    trackId: string;
    beat: number;
    durationBeats?: number;
    value?: number;
    type?: string;
    data?: Record<string, unknown>;
};

// @public
export type TimelineEventValidator<TEvent extends TimelineEvent = TimelineEvent> = (event: TEvent) => void;

// @public (undocumented)
export type TimelineMigrationOptions = {
    durationBeats: number;
    onRemovedField?: (event: TimelineEventV1) => JsonObject | undefined;
};

// @public (undocumented)
export type TimelinePlaybackEvent = Omit<EnginePlaybackEvent, "snapshot"> & {
    snapshot: TimelinePlaybackSnapshot;
};

// @public (undocumented)
export type TimelinePlaybackSnapshot = EnginePlaybackSnapshot;

// @public (undocumented)
export type TimelineProject<TEvent extends TimelineEvent = TimelineEvent> = {
    version: 2;
    timing: TimingMap;
    durationBeats: number;
    tracks: TimelineTrack[];
    events: TEvent[];
};

// @public (undocumented)
export type TimelineProjectEvent<TEvent extends TimelineEvent = TimelineEvent> = {
    project: TimelineProject<TEvent>;
    previousProject: TimelineProject<TEvent>;
    positionMs: number;
    beat: number;
};

// @public (undocumented)
export type TimelineProjectV1 = {
    version: 1;
    timing: TimingMapV1;
    tracks: TimelineTrackV1[];
    events: TimelineEventV1[];
};

// @public (undocumented)
export type TimelineQueryOptions = {
    trackIds?: string[];
    includeDisabledTracks?: boolean;
    includeGlobalEvents?: boolean;
    eventTypes?: string[];
};

// @public (undocumented)
export type TimelineTrack = {
    id: string;
    name: string;
    enabled: boolean;
    data?: JsonObject;
};

// @public (undocumented)
export type TimelineTrackV1 = {
    id: string;
    name: string;
    enabled: boolean;
    type?: string;
    data?: Record<string, unknown>;
};

// @public (undocumented)
export type TimingMap = {
    tempos: TempoEvent[];
    startPositionMs: number;
};

// @public (undocumented)
export type TimingMapV1 = {
    tempos: TempoEventV1[];
    offsetMs: number;
};

// @public
export const toggleStep: (project: SequenceProject, trackId: string, stepIndex: number) => SequenceProject;

// @public (undocumented)
export type Track = {
    id: string;
    name: string;
    enabled: boolean;
    steps: StepValue[];
};

// @public (undocumented)
export type TransportEvent = {
    type: "start";
    bpm: number;
    stepIndex: number;
    timestamp: number;
} | {
    type: "stop";
    bpm: number;
    stepIndex: number;
    timestamp: number;
} | {
    type: "reset";
    bpm: number;
    stepIndex: number;
    timestamp: number;
} | {
    type: "bpm";
    bpm: number;
    previousBpm: number;
    stepIndex: number;
    timestamp: number;
};

// @public
export const unionTrackIds: (arrangement: ArrangementProject) => string[];

// @public (undocumented)
export type Unsubscribe = () => void;

// @public (undocumented)
export const updateTimelineEvent: (project: TimelineProject, eventId: string, patch: Partial<TimelineEvent>, eventValidator?: TimelineEventValidator) => TimelineProject;

// @public
export const validateArrangement: (input: unknown) => ValidationResult;

// @public (undocumented)
export const validateProject: (input: unknown) => ValidationResult;

// @public (undocumented)
export const validateTimelineProject: (input: unknown, eventValidator?: TimelineEventValidator) => ValidationResult;

// @public
export const validateTimingMap: (timing: TimingMap) => void;

// @public (undocumented)
export type ValidationIssue = {
    path: string;
    message: string;
};

// @public (undocumented)
export type ValidationResult = {
    ok: true;
    errors: [];
} | {
    ok: false;
    errors: ValidationIssue[];
};

// Warnings were encountered during analysis:
//
// dist/index.d.ts:210:5 - (ae-forgotten-export) The symbol "MigrationIssue" needs to be exported by the entry point index.d.ts

// (No @packageDocumentation comment for this package)

```
